# OriginFD Core Dependencies
# Shared dependencies across all services with pinned versions

# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
# Production WSGI server
gunicorn==22.0.0

# Database
sqlalchemy==2.0.23
alembic==1.13.1

# Authentication and security
# Note: python-jose[cryptography] includes cryptography as dependency
python-jose[cryptography]==3.3.0
# Explicit PyJWT for direct JWT operations (fixes ModuleNotFoundError)
PyJWT==2.8.0
# Explicit pin to ensure compatible version
cryptography==42.0.8

# Validation and serialization
pydantic[email]==2.5.0
pydantic-settings==2.1.0

# Environment and configuration
python-dotenv==1.0.0

# Date and time utilities
python-dateutil==2.8.2

# HTTP client
httpx==0.25.2
requests==2.31.0

# Task queue and async processing
# Fix: Use compatible versions to avoid dependency conflicts
celery[redis]==5.4.0
redis==4.6.0

# AI/ML libraries
openai==1.3.7
anthropic==0.7.8
numpy==1.25.2

# Development tools
pytest==7.4.3
pytest-asyncio==0.21.1